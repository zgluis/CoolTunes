import Foundation

struct Constants {
    struct Domains {
        static let production = "https://itunes.apple.com/"
    }

    struct SearchSongs {
        static let limit = "20"
        static let mediaType = "music"
    }
    static let baseDomain = Domains.production
}
